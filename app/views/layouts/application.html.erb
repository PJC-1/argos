<!DOCTYPE html>
<html>
<head>
  <title>Argos</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <%= stylesheet_link_tag "application", 'https://fonts.googleapis.com/css?family=Work+Sans:400,100,900', :media => "all" %>
</head>
<body>
<nav class="navbar navbar-default navbar-static-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <a><%= link_to "argos", root_path, class: "navbar-brand"%></a>
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav navbar-form pull-right navright" role="search">
        <% if current_user %>
        <li><%= link_to "Add A Pet", new_user_pet_path(current_user) %></li>
        <li><%= link_to "Profile", user_path(current_user)%></li>
        <li><%= link_to "Edit", edit_user_path(current_user)%></li>
        <li><%= link_to "Logout", logout_path %></li>
        <% else %>
        <li><%= link_to 'Login', login_path %></li>
        <li><%= link_to 'Signup', new_user_path %></li>
        <% end %>
        <li><%= link_to "Users", users_path%></li>
      </ul>
    </div>
  </div>
</nav>
<header>
    <div class="header-content">
        <br>
        <div class="header-content-inner">
            <br>
            <h1>Argos</h1>
            <hr>
        </div>
    </div>
</header>

<% flash.each do |name, msg| %>
  <p>
    <small> <%= name.capitalize %>: <%= msg %> </small>
  </p>
<% end %>

<%= yield %>

</body>
</html>
