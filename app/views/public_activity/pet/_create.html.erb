added pet

<% if activity.trackable %>
  <%= link_to activity.trackable.name, "/users/#{activity.trackable.user_id}/pets/#{activity.trackable.id}" %>
<% else %>
  which has since been removed
<% end %>
