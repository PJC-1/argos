
started following

<% if activity.trackable %>
  <%= link_to cl_image_tag(activity.trackable.followable.avatar_url(:thumbnail)), "/users/#{activity.trackable.followable_id}" %>
  <%= link_to activity.trackable.followable.first_name, "/users/#{activity.trackable.followable_id}" %>
<% else %>
  which has since been removed
<% end %>
