<h1>Friend's Activities</h1>
<% @activities.each do |activity| %>
  <div class="activity">
    <%= link_to activity.owner.first_name, activity.owner if activity.owner %>

    <%= render_activity activity %>
  </div>
<% end %>
<br>
<hr>
<br><%= link_to "back to profile", user_path(current_user)%>
